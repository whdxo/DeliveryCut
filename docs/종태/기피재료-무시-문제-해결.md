# 기피재료 무시 문제 해결 작업

## 문제 상황
- 사용자가 "감자"를 기피재료로 설정해도 AI가 감자 포함 메뉴를 추천함
- 현재 프롬프트에는 기피재료 규칙이 있지만 강조가 부족함

## 원인 분석
1. **규칙 위치**: `RULES_PROMPT` 23번 줄에 기피재료 규칙이 있지만 다른 규칙들 사이에 묻혀 있음
2. **프롬프트 순서**: SYSTEM → RULES → USER 순서로 전달되는데, 기피재료가 USER 프롬프트에서 한 줄로만 언급됨
3. **강조 부족**: "절대 제외"라는 표현만으로는 AI가 우선순위를 낮게 판단할 가능성

## 해결 계획

### 1단계: RULES_PROMPT 강화
- 기피재료 규칙을 최상단으로 이동
- "⚠️ 최우선 규칙" 섹션 신설
- 구체적인 검증 기준 추가

### 2단계: buildUserPrompt 개선
- 기피재료 섹션을 별도 강조 블록으로 분리
- "다음 재료가 포함된 메뉴는 추천하지 마세요" → "절대 금지" 표현으로 강화

### 3단계: SYSTEM_PROMPT 보완
- 역할 정의에 "사용자의 식품 알레르기/기피 식재료를 철저히 준수" 추가

---

## 작업 완료

### ✅ 수정된 파일: `lib/ai/prompts.ts`

#### 변경 1: SYSTEM_PROMPT에 기피재료 준수 명시
```typescript
export const SYSTEM_PROMPT = `
당신은 1인 가구를 위한 "현생 요리 플래너"입니다.

# 역할
- 사용자의 시간·도구·재료 제약을 기반으로 배달 대신 직접 만들 수 있는 메뉴 추천
- 재료 낭비를 방지하는 3일 식단 플랜 제공
- 간결하고 실패 확률 낮은 레시피 제공
- **사용자의 식품 알레르기 및 기피 재료를 절대적으로 준수**

# 출력 규칙
1. JSON만 반환 (설명, 마크다운, 코드블록 금지)
2. 모든 필드를 빠짐없이 채울 것
3. 한국어로 작성
`.trim()
```

#### 변경 2: RULES_PROMPT 최상단에 기피재료 규칙 배치
```typescript
export const RULES_PROMPT = `
# ⚠️ 최우선 규칙 (절대 위반 금지)

**기피 재료 (dislikedIngredientsText)가 주어진 경우:**
- 해당 재료가 포함된 메뉴는 menuOptions에 절대 포함하지 말 것
- 해당 재료가 ingredients 배열에 절대 포함되지 않도록 할 것
- 해당 재료가 steps(조리 과정)에도 언급되지 않도록 할 것
- 해당 재료의 파생 재료(예: "감자" → "감자전분", "감자칩")도 제외
- threeDayPlan과 shoppingList에도 해당 재료 절대 금지

예시: "감자"가 기피 재료일 경우 → 감자볶음, 감자조림, 감자전, 감자샐러드 등 모든 감자 메뉴 제외

---

# 반드시 지킬 규칙

## 제약 조건
- timeMin: 모든 메뉴는 입력된 timeLimitMin 이하여야 함
- tools: 각 메뉴의 tools는 입력된 tools 목록 안에서만 선택
- 유통기한 임박 재료 (D-3 이하): 해당 재료를 반드시 활용하는 메뉴를 우선 추천

## 출력 형식
- menuOptions: 정확히 3개, optionId는 "option-1", "option-2", "option-3"
- 메뉴 3개는 서로 다른 형태 (예: 볶음밥/덮밥/찜 등)
- steps: 각 메뉴당 최소 3줄, 최대 5줄, 간결하게
- threeDayPlan: 정확히 3일 (day: 1, 2, 3)
- shoppingList: 현재 재료에서 부족한 것만

## 품질 기준
- 설거지 최소 (한 냄비/팬으로 끝나는 메뉴 우선)
- tip은 "가장 흔히 실패하는 포인트" 한 줄
- 재료는 있는 것 최대한 활용
- 재료 수량이 부족하면 그 재료 없이 만드는 대안 메뉴도 고려
`.trim()
```

#### 변경 3: buildUserPrompt에서 기피재료 강조
```typescript
export function buildUserPrompt(input: GenerateInput): string {
  const { timeLimitMin, tools, ingredientsText, dislikedIngredientsText, fridgeContext, recentMenus } = input

  const toolNames: Record<string, string> = {
    microwave: "전자레인지",
    pan: "팬",
    airfryer: "에어프라이어",
  }

  // fridgeContext가 있으면 수량+유통기한 포함한 상세 재료 목록 생성
  let ingredientsSection: string
  if (fridgeContext && fridgeContext.length > 0) {
    const lines = fridgeContext.map((item) => {
      const expiryNote =
        item.daysLeft === null || item.daysLeft === undefined
          ? "유통기한 없음"
          : item.daysLeft <= 0
          ? "만료됨 (최대한 빨리 사용)"
          : item.daysLeft <= 3
          ? `유통기한 D-${item.daysLeft} (빨리 써야 함)`
          : `유통기한 D-${item.daysLeft}`
      return `- ${item.name} ${item.amount}${item.unit} (${expiryNote})`
    })
    ingredientsSection = `냉장고 재료 (수량·유통기한 포함):\n${lines.join("\n")}`
  } else {
    ingredientsSection = `냉장고 재료: ${ingredientsText}`
  }

  const recentMenusSection =
    recentMenus && recentMenus.length > 0
      ? `\n- 최근에 만든 메뉴 (중복 추천 금지): ${recentMenus.join(", ")}`
      : ""

  // 기피 재료 강조 블록
  const dislikedSection = dislikedIngredientsText
    ? `
⚠️ **절대 사용 금지 재료**
다음 재료는 알레르기/기피 사유로 절대 사용할 수 없습니다:
${dislikedIngredientsText}

이 재료가 포함된 메뉴는 추천하지 마세요. 재료 목록, 조리 과정, 식단 플랜 어디에도 포함되어서는 안 됩니다.
`
    : ""

  return `
# 사용자 조건

- 요리 시간: ${timeLimitMin}분 이내
- 사용 가능한 도구: ${tools.map((t) => toolNames[t] ?? t).join(", ")}
- ${ingredientsSection}${recentMenusSection}
${dislikedSection}
# 요청
위 조건으로:
1. 배달 대신 직접 만들 수 있는 메뉴 3가지
2. 각 메뉴의 재료, 조리 순서(3~5줄), 실패 방지 팁
3. 이 재료들로 3일간 돌려먹는 식단 플랜
4. 부족한 재료만 담은 장보기 리스트

JSON 스키마를 정확히 준수해서 반환하세요.
`.trim()
}
```

#### 변경 4: PLANNER_RULES_PROMPT도 동일하게 강화
```typescript
export const PLANNER_RULES_PROMPT = `
# ⚠️ 최우선 규칙 (절대 위반 금지)

**기피 재료가 주어진 경우:**
- 해당 재료가 포함된 메뉴는 전체 플랜 어디에도 포함 금지
- 해당 재료의 파생 재료(예: "감자" → "감자전분")도 제외
- dayPlans의 모든 meals와 shoppingList에서도 해당 재료 절대 배제

---

# 반드시 지킬 규칙

## 제약 조건
- 냉장고 재료를 먼저 소진하는 방향으로 메뉴 구성
- 전날 남은 재료를 다음날 메뉴에 재활용 (isLeftover: true로 표시)
- 같은 재료를 여러 끼니에 나눠 사용해 낭비 최소화

## 출력 형식
- dayPlans: 요청한 days 수만큼 (day 1부터 순서대로)
- 각 day의 meals 수 = mealsPerDay
- shoppingList: 냉장고에 없는 재료만
- totalEstimatedCost: 장보기 예상 금액 (원 단위 숫자)
- cookingTips: 전체 플랜에 대한 팁 2~3개 (배열)

## 품질 기준
- 각 메뉴의 timeMin은 현실적인 조리 시간 (최대 30분)
- 재료 효율을 위해 한 재료로 여러 끼니를 커버하는 구성 우선
`.trim()
```

#### 변경 5: buildPlannerUserPrompt도 기피재료 강조
```typescript
export function buildPlannerUserPrompt(input: PlannerInput): string {
  const { days, mealsPerDay, budget, fridgeIngredients, dislikedIngredientsText } = input

  const mealLabels: Record<number, string> = { 1: "저녁 1끼", 2: "점심·저녁 2끼", 3: "아침·점심·저녁 3끼" }

  const dislikedSection = dislikedIngredientsText
    ? `
⚠️ **절대 사용 금지 재료**
다음 재료는 알레르기/기피 사유로 절대 사용할 수 없습니다:
${dislikedIngredientsText}

이 재료가 포함된 메뉴는 전체 ${days}일 플랜 어디에도 포함하지 마세요.
`
    : ""

  return `
# 플래너 조건

- 기간: ${days}일
- 하루 끼니 수: ${mealLabels[mealsPerDay] ?? `${mealsPerDay}끼`}
${budget ? `- 예산: ${budget.toLocaleString()}원 이내` : "- 예산: 제한 없음"}
${fridgeIngredients ? `- 냉장고에 있는 재료 (우선 활용): ${fridgeIngredients}` : ""}
${dislikedSection}
# 요청
위 조건으로:
1. ${days}일 × ${mealsPerDay}끼니 식단 플랜
2. 재료를 효율적으로 공유하는 메뉴 구성
3. 냉장고에 없는 재료만 담은 장보기 리스트
4. 예상 식재료 비용 (원)
5. 전체 플랜 활용 팁 2~3개

JSON 스키마를 정확히 준수해서 반환하세요.
`.trim()
}
```

---

## 테스트 방법

### 1. Quick 모드 테스트
```bash
curl -X POST http://localhost:3001/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "timeLimitMin": 15,
    "tools": ["pan"],
    "ingredientsText": "당근, 감자, 양파, 달걀",
    "dislikedIngredientsText": "감자"
  }'
```

**기대 결과**: menuOptions 3개 모두 감자 없는 메뉴 (당근볶음, 달걀볶음 등)

### 2. Planner 모드 테스트 (향후 API 연동 시)
```bash
curl -X POST http://localhost:3001/api/planner/generate \
  -H "Content-Type: application/json" \
  -d '{
    "days": 3,
    "mealsPerDay": 2,
    "budget": 30000,
    "fridgeIngredients": "당근, 감자, 양파",
    "dislikedIngredientsText": "감자"
  }'
```

**기대 결과**: 3일 플랜 전체에서 감자 메뉴 제외

---

## 주요 변경 요약

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| **SYSTEM_PROMPT** | 기피재료 언급 없음 | "식품 알레르기 및 기피 재료를 절대적으로 준수" 추가 |
| **RULES_PROMPT** | 23번 줄에 한 줄 규칙 | 최상단 "⚠️ 최우선 규칙" 섹션 신설 (5줄 상세 설명) |
| **buildUserPrompt** | "기피 재료 (절대 제외): ..." 한 줄 | 별도 ⚠️ 블록으로 3줄 강조 |
| **파생 재료 처리** | 언급 없음 | "감자 → 감자전분" 예시로 파생 재료 제외 명시 |
| **Planner 프롬프트** | Quick과 동일한 수준 | Quick과 동일하게 강화 적용 |

---

## 예상 효과
1. **AI 우선순위 상승**: 최상단 "⚠️ 최우선 규칙"으로 기피재료 검증을 다른 조건보다 먼저 수행
2. **명확한 검증 기준**: "menuOptions, ingredients, steps, threeDayPlan, shoppingList 전체에서 제외" 명시
3. **파생 재료 처리**: "감자전분", "감자칩" 같은 파생 재료도 자동 제외
4. **일관성**: Quick/Planner 양쪽 모두 동일한 수준의 기피재료 처리

---

## 테스트 결과 (2026-02-20)

### 테스트 케이스: 감자 기피재료 설정
```bash
curl -X POST http://localhost:3001/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "timeLimitMin": 15,
    "tools": ["pan"],
    "ingredientsText": "당근, 감자, 양파, 달걀",
    "dislikedIngredientsText": "감자"
  }'
```

### ✅ 테스트 통과
**입력**: 냉장고에 "당근, 감자, 양파, 달걀" 있고, "감자"를 기피재료로 설정

**결과**: 감자 완전히 제외된 메뉴만 추천됨
1. **당근 달걀 볶음** (10분) - 재료: 당근, 양파, 달걀, 소금, 후추
2. **당근 양파 오믈렛** (15분) - 재료: 당근, 양파, 달걀, 소금, 후추, 치즈
3. **당근 양파 스크램블** (10분) - 재료: 당근, 양파, 달걀, 소금, 후추

**검증 항목**:
- ✅ menuOptions 3개 모두 감자 미포함
- ✅ ingredients 배열에 감자 없음
- ✅ steps(조리 과정)에 감자 언급 없음
- ✅ threeDayPlan 3일치 모두 감자 없는 메뉴만
- ✅ shoppingList에 감자 미포함

**응답 시간**: 약 11초

---

## 작업 완료 일시
2026-02-20

## 작업자
종태 (AI 연동 담당)
